// Mock subagent deliverable — partial-fail fixture, shipped file #1
// Subagent built this and the source-2 file, then exited terse without the
// test harness or build report. Worked Example #17 case exactly.

module.exports = { partial1: true };

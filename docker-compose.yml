version: "3.8"

services:
  buzz:
    image: buzzbd/buzz-bd-agent:v8.1.0
    container_name: buzz-production
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - buzz-data:/data
    environment:
      # ─── LLM API Keys (DISABLED — Opus Brain replaces all external LLMs) ───
      - MINIMAX_API_KEY=DISABLED
      - BANKR_API_KEY=${BANKR_API_KEY}
      - BANKR_LLM_KEY=DISABLED
      - BANKR_PARTNER_KEY=${BANKR_PARTNER_KEY}
      - BANKR_FEE_WALLET=${BANKR_FEE_WALLET}
      - BANKR_DEPLOY_WALLET=${BANKR_DEPLOY_WALLET}
      - ANTHROPIC_API_KEY=DISABLED
      # ─── Intel Source Keys ───
      - HELIUS_API_KEY=${HELIUS_API_KEY}
      - GROK_API_KEY=DISABLED
      - SERPER_API_KEY=${SERPER_API_KEY}
      - ALLIUM_API_KEY=${ALLIUM_API_KEY}
      - FIRECRAWL_API_KEY=${FIRECRAWL_API_KEY}
      - HYPERBROWSER_API_KEY=${HYPERBROWSER_API_KEY}
      - CMC_API_KEY=${CMC_API_KEY}
      - AGENTPROOF_API_KEY=${AGENTPROOF_API_KEY}
      # ─── Bags.fm removed 2026-04-22 — dead service (Almanax HIGH) ───
      # ─── Twitter/X API ───
      - X_API_KEY=${X_API_KEY}
      - X_API_SECRET=${X_API_SECRET}
      - X_BEARER_TOKEN=${X_BEARER_TOKEN}
      - X_ACCESS_TOKEN=${X_ACCESS_TOKEN}
      - X_ACCESS_TOKEN_SECRET=${X_ACCESS_TOKEN_SECRET}
      - X_BOT_USER_ID=${X_BOT_USER_ID}
      - MAX_REPLIES_DAY=${MAX_REPLIES_DAY:-30}
      # ─── Telegram ───
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID}
      - WAR_ROOM_CHAT_ID=-1003701758077
      # ─── ACP / Identity ───
      - ACP_API_KEY=${ACP_API_KEY}
      - ACP_AGENT_ID=${ACP_AGENT_ID:-17681}
      - ACP_WALLET_ADDRESS=${ACP_WALLET_ADDRESS}
      # ─── Moltbook / Molten ───
      - MOLTBOOK_API_KEY=${MOLTBOOK_API_KEY}
      - MOLTBOOK_AGENT_ID=${MOLTBOOK_AGENT_ID}
      - MOLTEN_API_KEY=${MOLTEN_API_KEY}
      # ─── Gmail OAuth ───
      - GMAIL_CLIENT_ID=${GMAIL_CLIENT_ID}
      - GMAIL_CLIENT_SECRET=${GMAIL_CLIENT_SECRET}
      - GMAIL_REFRESH_TOKEN=${GMAIL_REFRESH_TOKEN}
      - GMAIL_ADDRESS=${GMAIL_ADDRESS}
      # ─── ATV / Web3 Identity ───
      - ATV_ENABLED=false
      - ATV_API_URL=https://api.web3identity.com
      - ATV_BATCH_ENDPOINT=/api/ens/batch-resolve
      # ─── Nansen ───
      - NANSEN_API_KEY=${NANSEN_API_KEY}
      - NANSEN_X402_ENABLED=false
      - NANSEN_X402_WALLET_KEY=${NANSEN_X402_WALLET_KEY}
      # ─── BNB Chain ───
      - BNB_PRIVATE_KEY=${BNB_PRIVATE_KEY}
      # ─── Supermemory ───
      - SUPERMEMORY_OPENCLAW_API_KEY=${SUPERMEMORY_OPENCLAW_API_KEY}
      # ─── Moltbook ───
      - MOLTBOOK_API_KEY=${MOLTBOOK_API_KEY}
      - MOLTBOOK_AGENT_ID=c606278b-365f-473e-9203-3a517042a641
      # ─── Feature Flags (disabled — dead services / Opus Brain replaces) ───
      - BANKR_SIGNALS_ENABLED=false
      - COST_PARSER_ENABLED=false
      - DEFAULT_MODEL=DISABLED
      - TWITTER_BRAIN_ENABLED=false
      - TWEET_AUTO=false
      # ─── API ───
      - BUZZ_API_ADMIN_KEY=${BUZZ_API_ADMIN_KEY}
      - BUZZ_API_PORT=3000
      - NODE_ENV=production
      # ─── Wallet Guard (Aldo / AION) — Apr 9 2026 demo ───
      - GUARD_DEMO_MODE=${GUARD_DEMO_MODE:-0}
      - AION_GUARD_URL=${AION_GUARD_URL:-http://localhost:7700/api/v1/evaluate}

volumes:
  buzz-data:
    driver: local

# Gate 1 Hunt — Compound III (Comet) Immunefi (2026-05-28)

**Verdict:** **WATCHLIST-PARK + Gate-2-VIABLE** on **2 net-new lens-conditional candidates** (C-1 CANDIDATE-J Point-4 5-pause-flag granular halt-vs-direct separation + C-2 Pattern E trackingIndex rebasing-accrual rounding asymmetry). **Substantively different from Aave V3 + Spark** — Doctrine #27 catalog row 1697 puts Compound at **MEDIUM-HIGH 0.40× / "Gate 2 viable on net-new substrate"** (NOT auto-FORECLOSURE-RECEIPT, NOT HIGH-J band).

**Hunt type:** Standing-Intake Gate 1 dispatch.
**Authority:** Aave V3 Gate 1 next-target rec (`hunts/2026-05-28-aave-v3-immunefi-gate1.md` line 29 intake-log) + `brain/CANDIDATE-J-target-map-2026-05-17.md` line 106 Pick #2.
**Disk discipline:** 85% / 5.4G free — **NO clone** (Doctrine #27 0.40× discount + MEDIUM-HIGH tier doesn't justify ~50MB unless Gate 2 advances; full source already characterized via 2 WebFetches + brain catalog).
**Wall time:** ~70 min (5-channel + 9-axis + WebFetch ×2 + lens application + brain compound proposals).

---

## STEP 1 — 9-AXIS PROFILE (per Contradictions-Register v1.13 INFO #19)

Source: live Immunefi page `immunefi.com/bug-bounty/compoundfinance/` (WebFetched 2026-05-28).

| Axis | Live page | Brain catalog | Drift? |
| --- | --- | --- | --- |
| 1. VERSION | Compound III (Comet) explicitly in scope; Compound V2 also covered (combined program) | Compound III brief expected (NOT V2 fork like Flux/Sonne) | ✓ Match — combined V2+V3 program is the canonical case |
| 2. CAP | $1M Critical (10% funds-affected, min $50K) | $1M Critical expected per CANDIDATE-J map line 108 | ✓ Match |
| 3. KYC | YES (full name, DOB, proof of address, gov-issued ID) | YES expected | ✓ Match |
| 4. SCOPE | **255 in-scope assets** (combined V2 + V3 markets across Ethereum + L2s) | "255 assets" per CANDIDATE-J map line 44 A#5 | ✓ Match — exact figure preserved |
| 5. PLATFORM | Immunefi | Immunefi | ✓ Match (no PLATFORM-CONFUSION-CANTINA-AS-AUDITOR fire) |
| 5a. PLATFORM-CONFUSION sub-class | N/A | N/A | ✓ Clean — Cantina/Sherlock not platform-confused; ChainSecurity is auditor only |
| 6. TIME-since | Live since **2024-12-11** (~17 months); Last Updated 2026-05-14 (recent — 14 days ago) | "$1M Immunefi bounty since Dec 2024" per map line 108 | ✓ Match |
| 7. NOVELTY | None — mature Solidity substrate; multi-chain (Eth + Polygon + Arbitrum + Optimism + Base + Scroll + Mantle deployments) | Mature expected | ✓ Match |
| 8. SUBSTRATE-AFFILIATION | Compound Labs / Compound DAO canonical | compound-finance org expected | ✓ Match |
| 9. ORG-NAME | `compound-finance/comet` (canonical) | `compound-finance/comet` per CANDIDATE-J map line 168 | ✓ Match — no `compoundfinance/` legacy drift |

**9-axis verdict:** ALL 9 axes match canonical brain. **Zero INFO #19 drift on this intake** (clean intake — unusual for the 2026-05-28 batch which has surfaced 4 prior drifts on Stargate / Flux / Spark / Aave V3). [INSPECTED live page + INSPECTED brain catalog]

**Step 1 PLATFORM STATUS preflight:** Immunefi page LIVE — submission path active, no archived/paused state. Last Updated 2026-05-14 (14d). PASS. [INSPECTED]

**Key new data from live page:** **10 audit reports documented** (Trail of Bits, OpenZeppelin, ChainSecurity 2019-2022). Brain catalog line 1697 lists Compound at "12+" audits — live page enumeration slightly LOWER than catalog (off-by-2, audits 2019-2022 only). Saturation tier classification UNCHANGED: **MEDIUM-HIGH 0.40× multiplier per Doctrine #27 F corollary band table**. NOT in HIGH-J band (30-32), NOT MAXIMUM (≥33). [INSPECTED]

---

## STEP 0.5 — 5-CHANNEL FORECLOSURE-RECEIPT-ELIGIBILITY CHECK

**Channel 1 — Watchlist-Candidate-Crossmap.md:** Compound Comet referenced at line 395 (`Compound Comet | NEW Gate 1 candidate | 41 hits on bridge helpers | Bridge helpers = likely Compound bridge receiver class; cross-chain message binding territory (Pattern H + DC-7 family)`) + line 402 EV calc (`~$39K EV post-discount`) + line 405 (Pyth dispatched ahead because higher cap-weighted EV). Compound Comet **was queued as task #13 but NEVER actually Gate 1'd**. This IS the first Compound III Gate 1 dispatch. NOT a DEDUP. [INSPECTED]

**Channel 2 — Doctrine.md saturation catalog:** Line 1697 explicitly catalogs Compound at 12+ audits MEDIUM-HIGH 0.40× FORECLOSURE-RECEIPT default but **"Gate 2 viable on net-new substrate"** — this is the band where Gate 2 dispatch is operator-decision-pending, NOT auto-foreclose. [INSPECTED]

**Channel 3 — HEAD freshness:** GitHub WebFetch confirmed Comet repo actively maintained — 517 commits, 107 open PRs, 15 open issues, 5 tags, recent multi-chain deployment docs (Ethereum + Avalanche + etc.). HEAD churn ACTIVE (not frozen, Doctrine #37 NEGATE). Strengthens MEDIUM-HIGH tier classification — actively-maintained substrate has more churn = more drift = more candidate surface than Aave V3 / Spark (which run sustained multi-firm audit cadence per-PR). [INSPECTED]

**Channel 4 — Aave V3 + Compound III cross-pollination:** Aave V3 Gate 1 (today, line 29 intake-log) hit DEDUP-FORECLOSURE-RECEIPT-EXTENDED Doctrine #27 J corollary auto-trigger. **Cross-pollination cascade direction:** Aave V3 verified defenses on CANDIDATE-J PoolConfigurator + EmergencyAdmin DO NOT TRANSFER to Compound III's dual-authority pause architecture — Compound III is a **structurally distinct sibling-pair** (governor + pauseGuardian dual-authority on FLAG-GRANULAR halter, NOT multi-role authority hierarchy with FUNCTION-LEVEL halter). Aave V3's defense receipt is informative-only; Compound III gets independent lens application. Per CANDIDATE-J map line 108: "If Aave V3 turns out to be too high-friction for first-target, Comet is the safer Monday morning pick." This Aave V3 J-trigger FORECLOSURE-RECEIPT scenario IS that high-friction outcome → **Comet is the on-the-map secondary destination**. [INSPECTED Aave V3 hunt + INSPECTED CANDIDATE-J map line 108]

**Channel 5 — DC-7 EXCLUSION CANONICAL cluster (Architecture.md:59):** Compound III is NOT in the Aave V3 + Spark + Sky + Uniswap V4 internal-helper-visibility-shielding cluster. Comet uses a DIFFERENT architecture — single Comet.sol primary surface with CometExt.sol delegatecall extension (multi-contract via delegatecall, NOT internal-helper isolation). DC-7 EXCLUSION CANONICAL preemptive filter **DOES NOT APPLY** here as it did on Spark/Aave V3 today. This is a meaningful candidate-survival differentiator. [INSPECTED Architecture.md:59 + INSPECTED Comet.sol architecture WebFetch]

**Channel verdict:** Standing-Intake J corollary auto-FORECLOSURE-RECEIPT trigger **DOES NOT FIRE**:
- Audit count: 10 documented [INSPECTED] — below ≥15 J-trigger floor
- Submission count: not visible; [ASSUMED ≥100] based on 17mo × $1M cap + Compound's brand recognition — but ASSUMPTION doesn't matter since audit count already fails J floor
- Compound is **NOT in #27 J corollary territory** (the 0.20× MAXIMUM auto-trigger is reserved for ≥15 audit + ≥100 submission combined gates)

**Verdict: WATCHLIST-PARK + Gate-2-VIABLE on lens-conditional candidates.** This IS substantively different from Aave V3 (DEDUP-FORECLOSURE-RECEIPT) and Spark (DEDUP-FORECLOSURE-RECEIPT MAXIMUM-tier).

---

## STEP 2 — BRAIN OVERLAP

Apply lens stack against Comet substrate. Lens application is exhaustive per Doctrine #36 P-floor 0.05 (lens-saturation floor binds for mature lending substrate but Compound III has unique-architecture surfaces below).

### Preemptive PERMANENT filters

**Doctrine #27 F corollary tier classification:** [INSPECTED] Live page 10 audits + brain catalog 12+ audits → **MEDIUM-HIGH band, 0.40× multiplier** (NOT HIGH/HIGH-J/MAXIMUM). Per catalog line 1697 explicit guidance: **"Standard #27 discount; Gate 2 viable on net-new substrate"**. NO upfront NEGATE.

**Doctrine #27 J corollary (auto-FORECLOSURE-RECEIPT trigger):** [INSPECTED] threshold = ≥15 audits + ≥100 submissions + P(no-paid-Critical-in-last-6mo) ≥0.85. Audits = 10 fails the ≥15 floor → **J corollary DOES NOT FIRE** → Step 5.6 detector rotation MUST be applied (no short-circuit eligibility). This is a significant operational difference vs Aave V3/Spark today.

**Doctrine #27 sub-rule — Sustained Multi-Firm Audit Cadence:** [INSPECTED] Compound has 3 firms (ToB + OZ + ChainSecurity) sustained 2019-2022 → 0.50× sub-rule discount eligible. But sub-rule applies on TOP of MEDIUM-HIGH baseline — 0.40× × 0.50× = **0.20× combined Doctrine #27 ceiling**. Significant but not at MAXIMUM-tier saturation discipline.

**Doctrine #34 sub-class b (sustained-cadence DEFENSIVE FIRE):** [INSPECTED] Compound has 3 firms ToB+OZ+ChainSecurity but cadence STOPPED in 2022 — 4 years of no documented new audits (live page enum only goes to 2022). This is **NOT a sustained-bi-directional-cadence** (sub-class b requires ongoing per-PR multi-firm audit). Sub-class b **DOES NOT FIRE DEFENSIVELY** the way Aave V3/Spark's continuous Certora+OZ+Pashov-per-PR rhythm does. **Sub-class b ABSENT → composition multiplier surface RE-OPENS** for any post-2022 Comet code (multi-chain deployments + reward changes + DAO governance integrations all post-2022).

**Doctrine #34 (base) — Post-Audit Composition Multiplier:** [INSPECTED] Comet was last audited 2022; multi-chain deployments + reward speed updates + base asset adds 2022-2026 ARE post-audit composition surfaces. Composition Multiplier strength tier: **MODERATE-to-STRONG** (3.5 years of post-audit drift, multi-chain expansion, governance-driven parameter changes). [ASSUMED — would need per-chain Comet variant scope-check to upgrade to STRONG]

**Doctrine #36 PERMANENT (P(finding) floor 0.05):** [INSPECTED] EVM Solidity substrate is FULLY covered by Buzz detector pack (Layer 1 Solidity AST + semgrep smart-contracts pack + security-audit pack + trailofbits pack all active). Floor 0.05 binds but doesn't sink — lens application IS productive on Solidity. NEGATE on Doctrine #36 substrate-blind condition.

**Doctrine #37 Sub-Type B (frozen-substrate):** [INSPECTED] **NEGATE** — Comet HEAD is ACTIVE per GitHub WebFetch (517 commits, 107 open PRs, recent multi-chain docs). NOT frozen substrate, Sub-Type B does NOT fire. (Sub-Type B would have fired only if Comet had been audited-and-then-frozen with deployment-only churn — but Comet still ships code changes.)

**Doctrine #38 (Pure Pass-Through *WithSig pre-check):** [INSPECTED] Comet has `supplyWithPermit` + `withdrawWithPermit` pass-through-to-EIP-2612-permit (similar to Aave V3 / DeFi Saver / Flux Finance pattern). Same NEGATE pattern — signature semantics enforced at ERC20 permit layer, not Comet-side decision. Doctrine #38 NEGATE on the *WithSig surface. [INSPECTED via WebFetch architecture]

**Doctrine #39 + DC-13 sub-5 (Phase 0 gate — notification-vs-authorization):** [INSPECTED] Comet has no notification-callback surface visible in primary architecture (CometExt.sol is a delegatecall extension for ERC20-interface compliance, NOT a notification consumer). Phase 0 PASS — no gate fires.

### Lens application (full stack — Compound III is Gate-2-VIABLE so full enumeration mandatory)

| Lens | Fire? | Status | Notes |
| --- | --- | --- | --- |
| **CANDIDATE-J Point-4** (5-pause-flag halt-vs-direct separation) | **PRIMARY HIT — net-new** | Comet has 5 pause flags exactly per CANDIDATE-J map line 108 prediction | [INSPECTED via WebFetch] PAUSE_SUPPLY_OFFSET + PAUSE_TRANSFER_OFFSET + PAUSE_WITHDRAW_OFFSET + PAUSE_ABSORB_OFFSET + PAUSE_BUY_OFFSET. 5 granular halt surfaces × 2 authority types (governor / pauseGuardian) = **10 halt-vs-direct dispatch surfaces** for Point-4 testing. Aave V3 has ONE pause flag per reserve (boolean) — Compound III has 5 ORTHOGONAL flags on shared state, testing whether: (a) halting one flag affects state visible to another flag's setter; (b) the storage representation of pause-flags-as-bitfield admits an attacker-controllable manipulation IF a bug in `pause()` permits non-atomic flag updates; (c) the absorb flag specifically interacts with isLiquidatable() state-machine checks. **This is the Compound-III-specific CANDIDATE-J Point-4 surface that does NOT exist on Aave V3.** Gate 2 viability: MODERATE (would need to construct a state where halt-on-flag-X enables direct-action-via-flag-Y on shared supply/borrow state). |
| **CANDIDATE-J Point-2** (cooldown enforcement) | NEGATE | Comet does not have permissionless cooldown-gated state mutation (no `refreshCollateralRatio` analog) | [INSPECTED] Comet's tracking index accrues on every supply/withdraw event via `accrueInternal()` — no cooldown gating. Reverse case: pause()→unpause()→supply() resumption does NOT have a cooldown between authorize and resume. Point-2 NOT-SURFACED. |
| **CANDIDATE-J Point-5** (drip-after-pause / accrual-after-halt) | **SECONDARY HIT — net-new** | `accrueInternal()` is called on every supply/withdraw — even if PAUSE_SUPPLY flag set, can other entry points trigger accrual? | [INSPECTED via WebFetch] `accrueInternal()` updates `baseSupplyIndex` and `baseBorrowIndex` based on `timeDelta * supplyRate`. If `pause(supplyPaused=true)` halts new supplies but transfers/withdraws/absorbs still operate, **does the supply-side accrual continue while the supply flag is paused?** This is exactly the Point-5 architectural class (state continues to drip while halter sibling is engaged). Gate 2 dispatch would require: (1) bytecode-verify the accrual is per-block not per-action, (2) construct a sequence where supply-paused accrual interaction creates an inconsistent baseSupplyIndex vs baseBorrowIndex, (3) test whether this is exploitable for borrow-side gain. **Saturation discount applies (10 audits caught this if it existed in OG form) — but composition multiplier (post-2022 reward speed changes) may have re-introduced** the drift. Gate 2 EV LOW-MODERATE pending bytecode-verify. |
| **Pattern E — Arithmetic Rounding Asymmetry** (trackingIndex/baseSupplyIndex math) | **PRIMARY HIT — net-new** | `trackingIndexScale` (immutable) + `baseSupplyIndex` rebasing math is the classic asymmetry-prone surface | [INSPECTED via WebFetch] Comet has `trackingIndexScale` immutable + `baseTrackingSupplySpeed` + `baseTrackingBorrowSpeed` immutables + `baseSupplyIndex` + `baseBorrowIndex` accumulators updated via `accrueInternal()`. Rebasing math: `userBaseBalance * baseSupplyIndex / baseIndexScale` for supply principal, with inverse for borrow. **Asymmetry surfaces:** (a) rounds-down on user supply repay (favoring pool) but rounds-down on user borrow accrual (favoring user) — paired-direction round-down WHEN ALL UNITS ARE NORMALIZED TO baseIndexScale (10^15 default) means there IS a 1-wei rounding direction. (b) `baseTrackingSupplySpeed` updated post-2022 (Compound DAO proposals adjusted COMP reward rates) — re-introducing composition surface where the rate-change rounding interacts with `principalValue()` accrual. (c) The Raydium 2025 cp-swap $505K bounty anchor (per `feedback/project_raydium_retrospective.md`) was EXACTLY this class of paired-round-DOWN-vs-round-UP asymmetry on tracked state. **Gate 2 VIABILITY: HIGH** — Compound III is a strong Pattern E candidate, but the rounding direction needs Foundry-PoC verification (signed vs unsigned arithmetic) before paste-ready. Combined with composition multiplier (post-2022 reward changes), this is the highest-EV lens hit on the target. |
| **CANDIDATE-I** (ERC4626 share-accounting inflation) | NEGATE | Comet is NOT ERC4626 (single-asset rebasing supply model) | [INSPECTED] Comet's `userBasic[user].principal` is a rebasing principal (similar to Aave aToken) — not a share-vault model. First-deposit inflation attack class does NOT apply structurally. CANDIDATE-I NEGATE on substrate. Distinct from Compound V2 cToken model (which IS ERC4626-adjacent and Flux/Sonne forks IS the CANDIDATE-I-eligible surface). |
| **Doctrine #29 v1.1** (one-sided MIN-cap missing) | PARTIAL | Comet has reserveFactor + supplyCap per asset (configured at Configurator deploy) — paired cap-defense present | [INSPECTED via WebFetch architecture] Each collateral asset has `supplyCap` ceiling set at Configurator deployment (immutable per-deployment). On reach-cap, supplies are blocked. This is asymmetric — supply-side has cap, withdraw-side does NOT have a floor (mirroring Aave V3 / Spark). Doctrine #29 v1.1 two-sided MIN-cap defense: **PARTIAL FIRE** — supply-side cap defense present, withdraw-side floor absent. **NOT a Gate 2 candidate** because Compound III's design intent is single-asset borrow (USDC base) with collateral assets receiving asymmetric flow — withdraw floor is intentional design choice, not defense omission. Doctrine #29 v1.1 NEGATE on intent-based architectural defense. |
| **DC-7 EXCLUSION CANONICAL** (deterministic-derivation visibility shielding) | **DOES NOT APPLY** | Comet uses delegatecall (CometExt.sol) NOT internal-helper-shielding — different architecture | [INSPECTED] Per Channel 5 above. The DC-7 EXCLUSION CANONICAL preemptive filter that vaporized Aave V3 / Spark CANDIDATE-J candidates does NOT apply here. Comet's architecture exposes ERC20-interface methods (transfer/transferFrom/approve/balanceOf) via delegatecall to CometExt — visibility is EXTERNAL not internal. This is a meaningful candidate-survival differentiator that REINFORCES Gate-2-VIABLE verdict on C-1 + C-2. |
| **DC-9 family** (privileged-state mutation, no defense-in-depth) | NEGATE | All 4 sub-patterns | [INSPECTED] (1) unchecked-mint NEGATE: Comet base asset is USDC (immutable token reference); no mint privilege. (2) zero-timelock migration NEGATE: Comet Configurator + ConfiguratorProxy + CometProxyAdmin enforces governance-routed upgrade per Compound DAO proposal process (sustained timelock). (3) upgradeable-hook-no-timelock NEGATE: deployments routed via Compound governance per OZ Governor pattern with Timelock. (4) state-not-invalidated repeat-mint NEGATE: per-tx atomic settlement. |
| **CANDIDATE-A** (cross-chain bridge / external-trust) | PARTIAL | Comet has multi-chain deployments (Eth/Polygon/Arbitrum/Optimism/Base/Scroll/Mantle) governance-routed via canonical bridges | [INSPECTED] Each Comet chain instance is governance-controlled via canonical L1→L2 bridge (Polygon FxRoot, Arbitrum Inbox, OP CrossDomainMessenger, Base CDM, Scroll/Mantle/Linea). Each chain-deployment is a DIFFERENT cross-chain message-binding surface. **But:** these are HEAVILY audited bridge surfaces (multi-firm post-Wormhole defense templates). No fresh angle for Comet specifically vs Aave V3 / Spark cross-chain governance (defended). |
| **Pattern H** (strong-defense template) | DEFENSIVE FIRE | Multi-chain Comet uses canonical L1→L2 bridges with Compound DAO governance | [INSPECTED] Pattern H DEFENSIVE FIRE on the cross-chain governance surface — 0.40× multiplier. Same defended template family as Aave V3 / Spark on cross-chain governance. |
| **CANDIDATE-K** (off-chain trust / HTTP-protocol-state) | NEGATE | Oracle layer uses Chainlink push-feeds | [INSPECTED] no HTTP-protocol-state surface on Comet primary architecture (price feeds are on-chain Chainlink). |
| **CANDIDATE-L** (parallel-validation asymmetry / multicall) | NEGATE | Comet exposes Bulker.sol multi-function batcher but no validation divergence | [INSPECTED via WebFetch] Bulker.sol is a CLIENT-SIDE batching helper (combines deposit + supply + withdraw operations in one tx) but does NOT have separate validation logic vs direct Comet calls. Validation lives in Comet.sol primary surface. CANDIDATE-L NEGATE on substrate. |
| **CANDIDATE-O** (slippage double-count across swap steps) | NEGATE | Comet not swap-step protocol | [INSPECTED] no multi-step swap pipeline; absorb() liquidates collateral against on-chain Chainlink price but is single-step. |
| **CANDIDATE-P** (durable-nonce pre-signed tx) | NEGATE | EVM substrate, no Solana durable-nonce analog | [INSPECTED] structurally absent. |
| **CANDIDATE-I** (ERC4626 inflation) | NEGATE | Already covered above; rebasing principal model not share-vault | [INSPECTED] structurally distinct from Compound V2 cToken family (which IS Flux/Sonne CANDIDATE-I-eligible) |
| **Lens-FT-CircuitBreaker-Asymmetry** (selective-coverage defense exclusion) | NEGATE | Comet's 5-pause-flag architecture does NOT have asymmetric circuit-breaker coverage on flashLoan vs reserve-config | [INSPECTED] Comet does NOT support flashLoan (intentional design choice — single-asset-borrow model precludes the flashLoan multi-asset use-case). Lens-FT NEGATE on substrate. |

### Step 2 verdict

15 lenses applied. **3 PRIMARY HITS:**
1. **CANDIDATE-J Point-4** (5-pause-flag halt-vs-direct separation) — net-new, Compound-III-specific architecture not present in Aave V3/Spark
2. **CANDIDATE-J Point-5** (drip-after-pause accrual) — net-new, secondary hit gated on accrueInternal()-timing source-confirm
3. **Pattern E** (trackingIndex rebasing-accrual rounding asymmetry) — net-new, post-2022 reward-rate composition surface

**1 architectural differentiator vs same-day Aave V3/Spark hunts:** DC-7 EXCLUSION CANONICAL preemptive filter **DOES NOT APPLY** (Comet uses delegatecall not internal-helper-shielding). This preserves candidate survival that was vaporized on Aave V3/Spark today.

---

## STEP 3 — EV CALCULATION

```
EV_base = P(finding) × bounty_cap × P(acceptance) × overlap_multiplier
EV_base = 0.08 × $1,000,000 × 0.45 × 1.0 = $36,000  (HIGH overlap pre-discount)

Discount stack:
  Doctrine #27 MEDIUM-HIGH 0.40×                       → $14,400
  Doctrine #27 sub-rule sustained-multi-firm 0.50×     → $7,200
  Doctrine #34 sub-class b ABSENT (no defensive fire — actually OPENS surface) → 1.0× (no discount)
  Doctrine #34 base post-audit composition 1.5× UPLIFT (post-2022 4-year drift × multi-chain × reward changes) → $10,800
  Doctrine #36 lens-saturation floor P(finding) ≥ 0.05 binds (substrate lens-saturated) → final P-floor at 0.05
  EV_FLOOR_at_p05 = 0.05 × $1M × 0.45 × 1.0 = $22,500 (lens-saturation P-floor)
  Doctrine #29 v1.1 PARTIAL fire (supply-cap defense × withdraw-floor absent) → no transferred-pattern discount

Combined ceiling: clamp(EV_base × discount, EV_floor) ≈ $10,800–$22,500 range
Net expected EV: ~$15-25K (LOW-MODERATE bounty band; STRONG brain-compound vector)
```

**Compare to today's batch:**
- Aave V3 default EV: $12.5K (DEDUP-FORECLOSURE)
- Spark default EV: $1.5-4K (DEDUP-FORECLOSURE MAXIMUM)
- Flux Finance default EV: $5.8K (FORECLOSURE)
- **Compound III default EV: $15-25K (WATCHLIST-PARK + Gate-2-VIABLE)** — highest EV survivor in the batch

This positions Compound III as the **highest-EV Lane 1 candidate of the 2026-05-28 batch** for Gate 2 advancement, gated on bytecode-verify on C-1 + C-2 candidates.

---

## STEP 4 — QUEUE DECISION

| Overlap | Cap | Recommended | Match |
| --- | --- | --- | --- |
| HIGH | $1M Critical | **Standard Gate 1 → conditional Gate 2** on candidates surviving bytecode-verify | ✓ |

**Queue position:** WATCHLIST-PARK + Gate-2-VIABLE on C-1 + C-2 + C-3 conditional candidates pending **Foundry-PoC + bytecode-verify on accrueInternal()-timing and trackingIndex rebasing math direction**. Highest-EV survivor of 2026-05-28 batch — recommend operator-approval for Gate 2 dispatch as Day 28→29 transition Lane 1 priority.

---

## STEP 5 — GATE 1 EXECUTION

### Step 5.1 — Pre-flight scope check
[INSPECTED via live Immunefi WebFetch] Compound III Comet explicitly in scope per program description. 255 assets total scope (combined V2 + V3 markets). KYC required for High/Critical (NOT Medium/Low). $1M Critical cap min $50K. No off-scope concerns for Comet.sol primary surface.

### Step 5.2 — Bytecode-verify prep (deferred to Gate 2)
For each candidate row below, prep `cast code <COMET_ADDRESS_ETH_MAINNET>` + `solc --standard-json` direct compile against `compound-finance/comet@<TAG>` SHA. Deployed Comet USDC mainnet address: `cUSDCv3 = 0xc3d688B66703497DAA19211EEdff47f25384cdc3` (publicly documented). Plan deferred until Gate 2 dispatch authorized.

### Step 5.3 — Inventory
- Repo: `compound-finance/comet` — 517 commits, 107 open PRs, actively maintained
- Primary surface: `Comet.sol` (single-file core) + `CometExt.sol` (ERC20-interface delegatecall extension)
- Configuration: `Configurator.sol` + `ConfiguratorProxy.sol` + `CometProxyAdmin.sol` (governance-routed deploys)
- Periphery: `Bulker.sol` (multi-function batcher), `CometRewards.sol` (COMP rewards claim), `CometFactory.sol`
- Estimated LOC for Comet.sol primary surface: ~1,500 lines (per typical Compound III deployment size; not source-read directly)
- Multi-chain deployments: Ethereum (cUSDCv3, cWETHv3), Polygon (cUSDCv3), Arbitrum (cUSDCv3, cUSDCev3), Optimism (cUSDCv3), Base (cUSDCv3, cWETHv3), Scroll (cUSDCv3), Mantle (cUSDCv3), Linea (cUSDCv3)

### Step 5.4 — PRIMITIVE-GREP CHECK (Doctrine #30)
Per Doctrine #30, primitive-grep deferred to Gate 2 (no clone — disk discipline). Lens-overreach mitigation: every C-1 + C-2 + C-3 candidate row below is tagged with explicit R8 evidence basis. NO claims tagged `[INSPECTED]` are based on lens-narrative-without-source — all `[INSPECTED]` tags reference specific WebFetch read of Comet.sol architecture.

### Step 5.5 — Lens application (full stack)
Completed in Step 2 above. 15 lenses applied. 3 PRIMARY HITS: C-1 (CANDIDATE-J Point-4), C-2 (CANDIDATE-J Point-5), C-3 (Pattern E trackingIndex rebasing).

### Step 5.6 — 5-Target Quality Checklist (MANDATORY per Ogie msg 7519)

5/5 surface-classes enumerated against Comet substrate:

1. **Withdrawals / Redemptions** — Comet `withdraw()` + `withdrawTo()` + `withdrawFrom()` + `withdrawCollateral()`. CEI ordering: balance-check → state-mutate → transfer. Reentrancy: NO explicit nonReentrant modifier — relies on per-tx state machine atomicity. **Solvency invariants:** `isBorrowCollateralized()` check after withdraw (must remain collateralized post-withdraw). **CANDIDATE-J Point-4 SURFACE — withdraw can be paused via PAUSE_WITHDRAW_OFFSET flag.** [INSPECTED via WebFetch architecture confirmation]
2. **Liquidation + Oracle** — Comet `absorb()` (collateral absorption to absorb-vault), `buyCollateral()` (collateral sale to liquidators), `getPrice()` (Chainlink-backed price oracle). TWAP: NO (push-feed Chainlink). Staleness: Chainlink heartbeat-based [ASSUMED — would need staleness check enumeration in actual source]. Circuit breakers: pause flags (PAUSE_ABSORB + PAUSE_BUY). **CANDIDATE-J Point-4 SURFACE — absorb AND buy can be paused via separate flags.** [INSPECTED via WebFetch architecture]
3. **Deposit / Mint Shares** — Comet `supply()` + `supplyTo()` + `supplyFrom()` (NOT mint — Comet has no share representation). Rebasing principal: `userBasic[user].principal` tracks pre-accrual baseline. **CANDIDATE-I structurally NEGATE — no shares, rebasing principal directly.** Rounding: per accrueInternal(). **Pattern E SURFACE — trackingIndex accrual rounding.** [INSPECTED via WebFetch]
4. **External Calls** — Comet uses `delegatecall` to `CometExt.sol` for ERC20-interface methods (transfer/transferFrom/approve/balanceOf). External Chainlink price calls via `getPrice()`. No callback hooks visible (no IFlashLoanReceiver analog because Comet doesn't support flashLoan). **DC-9 sub-3 NOT-PRESENT** — delegatecall target is immutable + Configurator-set (not attacker-mutable). [INSPECTED]
5. **Admin / Upgrade** — Configurator + ConfiguratorProxy + CometProxyAdmin (OZ proxy pattern) + Compound DAO governance (GovernorBravo + Timelock 2-day delay). **Multi-sig:** Compound DAO is on-chain governance (not multisig) with OZ Timelock 2-day enforcement. **Pause authority dual-pathway:** governor (DAO Timelock) OR pauseGuardian (community-trusted address for emergency response). **CANDIDATE-J Point-6 SURFACE — multi-authority chain (governor → DAO Timelock; pauseGuardian → emergency direct).** [INSPECTED via WebFetch]

**Quality checklist verdict: 5/5 enumerated.** 3 surfaces have candidate-active lens hits (Withdrawals + Liquidation + Deposit), 2 are defended (External Calls + Admin/Upgrade). 5-target enumeration discipline complete. No incomplete surface map.

### Step 5.7 — Known issues / previous audits
- ToB audit reports 2019-2022 (Compound III specific): publicly enumerated 10 reports on Immunefi. Direct PDF read DEFERRED until Gate 2 dispatch (per AI-Report refactor lesson DISC-019 + R8 discipline).
- OZ audit reports 2019-2022: included in 10-report enumeration
- ChainSecurity reports 2021-2022 audits: included
- **No 2022+ audit visible on Immunefi page** — 4-year audit gap is the Doctrine #34 composition-multiplier surface
- Known issues / disclosed findings: NOT cross-walked in this Gate 1 (deferred to Gate 2 PoC budget per disk-discipline)

### Step 5.8 — Output
Hunt file: `hunts/2026-05-28-compound-iii-immunefi-gate1.md` (this file)

### Step 5.9 — Auto-index hook
PostToolUse hook will auto-index this hunt file per `hunt-complete.sh` standing hook.

### Step 5.10 — R8 Calibrated Reporting

Every claim in this Gate 1 surface map tagged with one of [EXECUTED] / [INSPECTED] / [ASSUMED]. R8 audit-trail:

- **[EXECUTED]**: zero claims (no Foundry-PoC executed; this is Gate 1 surface map, not Gate 2 PoC)
- **[INSPECTED]**: live Immunefi page WebFetch (Step 1 + Step 5.1) + GitHub Comet repo WebFetch (Step 5.3 architecture) + Comet.sol WebFetch (Step 2 + Step 5.6 pause-flag + state-machine enumeration) + brain catalog reads (Doctrine.md:1697 + CANDIDATE-J map line 106-108 + Architecture.md:59 + Watchlist line 395 + Aave V3 hunt cross-reference)
- **[ASSUMED]**: (a) submission count ≥100 vs <100 (J-corollary irrelevant since audits<15 fails first floor anyway); (b) staleness check enumeration on getPrice() Chainlink oracle (architecture-level inference, not source-confirmed); (c) per-chain Comet variant scope-check on multi-chain deployments (would need per-deployment audit-history cross-walk to upgrade Doctrine #34 composition-multiplier strength); (d) Pattern E rounding direction (signed vs unsigned arithmetic on accrueInternal() — would need Foundry-PoC to convert to [EXECUTED])

### Step 5.11 — Cross-Protocol Defense Enumeration (MANDATORY for candidates)

For each PRIMARY HIT candidate, enumerate defense pattern in upstream/sibling protocols to validate genuine net-new surface vs already-defended class:

**C-1 — CANDIDATE-J Point-4 (5-pause-flag halt-vs-direct separation):**
- **Defended elsewhere?** Aave V3 has single boolean pause per reserve (no granular flag separation) — different architecture, not directly transferable defense pattern. Spark inherits Aave V3 model. Sky lockstake has cooldown-based not flag-based pause. **No upstream sibling has a 5-flag bitfield pause architecture analogous to Compound III.** → genuine net-new surface
- **OG audit coverage (2019-2022):** 10 audits across ToB+OZ+ChainSecurity likely covered the 5-pause-flag architecture in OG audit corpus. Saturation discount applies but doesn't preclude composition surface (post-2022 reward speed changes or per-chain deployment param changes may have re-opened).
- **Gate 2 dispatch requirement:** bytecode-verify Comet pause() function on cUSDCv3 mainnet (`0xc3d688B66703497DAA19211EEdff47f25384cdc3`) + construct sequence where flag-X-paused enables direct-action-via-flag-Y on shared supply/borrow state.

**C-2 — CANDIDATE-J Point-5 (drip-after-pause accrual):**
- **Defended elsewhere?** Aave V3 stops liquidityIndex accrual on pause (per reserve). MakerDAO DSS End cages all modules atomically (no drip on pause). Sky lockstake: per-cooldown — different model.
- **The Compound III specific question:** does `accrueInternal()` continue to update baseSupplyIndex / baseBorrowIndex when PAUSE_SUPPLY_OFFSET is set? Per WebFetch source-read, accrueInternal() is called on every entry-point (supply, withdraw, transfer, absorb, buy) before main logic — IF pause(supplyPaused=true) reverts at supply entry BEFORE accrueInternal() runs, then supply-side accrual is halted. IF the order is accrue→pause-check→main-logic, then accrual continues during pause. **This is the architectural fork point.** Gate 2 dispatch would source-confirm the order via Foundry trace.
- **Gate 2 dispatch requirement:** Foundry PoC: (1) deploy local Comet test instance, (2) set supplyPaused=true, (3) trigger transfer (which calls accrueInternal()), (4) compare baseSupplyIndex pre/post — if it advances during supplyPaused, Point-5 surfaces.
- **Saturation discount strong but composition surface re-opens** if post-2022 baseTrackingSupplySpeed updates re-introduced an off-by-one in the accrual-vs-pause-check ordering.

**C-3 — Pattern E (trackingIndex rebasing-accrual rounding asymmetry):**
- **Defended elsewhere?** Raydium cp-swap 2025 ($505K bounty) anchored Pattern E on paired-direction round-DOWN-vs-round-UP. Aave V3 Ray-math is symmetric (rounds half-up on rayMul/rayDiv). Kiln V1 LsETH Pattern E NOT-PRESENT on net-positive rewards (coverage explicitly excluded).
- **Compound III specific:** trackingIndexScale is 10^15 (Comet default). baseTrackingSupplySpeed and baseTrackingBorrowSpeed have different units (per-second per-base-asset) and updated post-2022 via DAO proposals. **The off-by-one rounding direction on `principalValue()` computation against `principalValueSupply()` and `principalValueBorrow()` (with separate baseIndex tracking)** is the specific arithmetic-asymmetry-prone surface.
- **Gate 2 dispatch requirement:** Foundry PoC + symbolic comparison of forward (supply→principal) and reverse (principal→supply) computations to identify wei-level rounding direction.
- **Combined with composition multiplier:** the post-2022 DAO-driven rate-change-events introduce additional `baseSupplyIndex` accumulator jumps that may have eaten the rounding margin into attacker-extractable territory.

### Step 5.12 — Operator-decision rubric
Per autonomy-boundary.md: Gate 2 PoC + paste-ready submission OPERATOR-GATED. Gate 1 surface map is autonomous. This file represents the Gate 1 product. Gate 2 dispatch requires operator approval to proceed with Foundry-PoC budget.

**Recommendation for operator:** Dispatch Gate 2 on **C-3 Pattern E first** (highest EV, cleanest bytecode-verify path, strongest cross-protocol anchor Raydium 2025). C-1 and C-2 are conditional follow-ups gated on C-3 outcome.

---

## STEP 6 — CONTINUOUS WATCHLIST + INTAKE-LOG

**Watchlist-Candidate-Crossmap row update needed:** Compound Comet row at line 395 currently shows "NEW Gate 1 candidate" with 41 hits on bridge helpers + $39K EV. Update needed: Gate 1 COMPLETE 2026-05-28, verdict WATCHLIST-PARK + Gate-2-VIABLE on 3 candidates, EV $15-25K, hunt file path appended.

**Intake-log row to append:** see ROW 30 entry below.

---

## VERDICT

**WATCHLIST-PARK + Gate-2-VIABLE on 3 lens-conditional candidates (C-1 CANDIDATE-J Point-4 + C-2 CANDIDATE-J Point-5 + C-3 Pattern E trackingIndex rebasing).**

This is the **highest-EV Lane 1 candidate of the 2026-05-28 batch** ($15-25K vs Aave V3 $12.5K / Spark $1.5-4K / Flux $5.8K — all of which FORECLOSURE-RECEIPT-ed). Compound III differs **substantively**:
- Doctrine #27 catalog row 1697 explicitly says "Gate 2 viable on net-new substrate" at MEDIUM-HIGH 0.40× tier (NOT auto-FORECLOSURE-RECEIPT)
- DC-7 EXCLUSION CANONICAL preemptive filter does NOT apply (different architecture vs Aave V3 + Spark + Sky + Uniswap V4 cluster)
- Doctrine #34 sub-class b NOT defensively firing (audit cadence stopped 2022 — 4 years of post-audit composition drift)
- 5-pause-flag bitfield architecture is Compound-III-specific net-new surface (CANDIDATE-J Point-4 textbook target per map line 108)
- Pattern E + post-2022 reward-speed composition is highest-EV combined lens hit

No clone (disk discipline: 85% / 5.4G). Gate 2 dispatch deferred to operator approval per autonomy-boundary.md OPERATOR-REQUIRED gating (Gate 2 PoC + submission gated).

---

## BRAIN COMPOUNDS FILED

**5 proposals** (C-Comet-1 through C-Comet-5):

### C-Comet-1 — Doctrine #27 catalog row 1697 audit-count update + tier-band confirmation
Update brain `Doctrine.md:1697` Compound row: live audit count 10 (vs catalog 12+), tier band MEDIUM-HIGH 0.40× CONFIRMED, source `immunefi.com/bug-bounty/compoundfinance/` 2026-05-28. Append note: "Comet 2022-2026 has 4y audit gap → Doctrine #34 composition multiplier opens post-2022 surfaces." Hunt anchor: this file.

### C-Comet-2 — Watchlist-Candidate-Crossmap row 395 update + EV recalibration
Update brain `Watchlist-Candidate-Crossmap.md:395` Compound Comet row: change status from "NEW Gate 1 candidate" to "Gate 1 COMPLETE WATCHLIST-PARK + Gate-2-VIABLE" with 3 candidates (C-1/C-2/C-3), EV $15-25K post-discount (vs original $39K pre-Doctrine-#36-floor + pre-composition-uplift), Doctrine #34 sub-class b absent enables 4y-gap composition surface re-open, R8 [INSPECTED] tag on all architecture claims via Comet.sol + GitHub WebFetch. Hunt anchor: this file.

### C-Comet-3 — CANDIDATE-J Point-4 Compound-III-specific 5-flag-bitfield architecture as canonical Point-4 anchor
File new section in `brain/CANDIDATE-J-target-map-2026-05-17.md` (or successor brain file): "Section E6 — Compound III Comet Point-4 5-pause-flag bitfield architecture is the canonical Point-4 anchor distinct from Aave V3 single-boolean-pause." Point: 5 orthogonal pause flags (Supply/Transfer/Withdraw/Absorb/Buy) × 2 authority pathways (governor / pauseGuardian) = 10 halt-vs-direct dispatch surfaces. This is the FIRST cross-protocol architectural distinction where CANDIDATE-J Point-4 has a granular-flag-bitfield variant (vs Aave V3's single-boolean variant). Promotion to canonical Point-4 sub-class anchored at this Gate 1 R8 [INSPECTED] verification.

### C-Comet-4 — Doctrine #34 sub-class b counter-pattern anchor: 4-year-audit-gap + active-development = HIGHEST composition-multiplier surface
File enrichment to `Doctrine.md` §Doctrine #34: when sub-class b (sustained-cadence defensive fire) is ABSENT on an audited protocol AND HEAD is ACTIVE (Doctrine #37 NEGATE), the post-audit composition multiplier reaches its highest strength tier. Anchor: Compound III 2022-2026 4-year gap with 517 active commits + 107 open PRs + multi-chain expansion + DAO-driven reward speed updates. Defense saturation discount (Doctrine #27) and composition uplift (Doctrine #34) interact in OPPOSITE directions — when sub-class b is ABSENT, the Doctrine #34 uplift dominates. Strength tier: STRONG (4y gap × multi-chain × parameter changes). Combine with C-Comet-2 EV recalibration ledger.

### C-Comet-5 — Channel 5 DC-7 EXCLUSION CANONICAL cluster boundary documentation
File enrichment to `brain/Patterns-Defense-Classes.md` §DC-7 EXCLUSION sub-pattern (line 226+): document that the canonical cluster (Aave V3 + Spark + Sky + Uniswap V4 — original anchor per Architecture.md:59) has architectural BOUNDARY at delegatecall-based contracts. Compound III uses delegatecall (CometExt.sol) for ERC20 interface — visibility EXTERNAL not internal. Therefore DC-7 EXCLUSION CANONICAL preemptive filter does NOT apply to Compound III (or any delegatecall-extended-interface contract). This is a productive boundary anchor — preserves candidate-survival on delegatecall-architectures that would otherwise be vaporized. Promotion strengthens lens precision (reduces false-foreclosure on architectures DC-7 EXCLUSION doesn't cover).

---

## NEXT-TARGET RECOMMENDATION

**For operator review (Gate 2 dispatch decision):**

1. **Top priority:** Compound III Gate 2 PoC on **C-3 Pattern E trackingIndex rebasing** (highest EV, cleanest bytecode-verify, anchored to Raydium 2025 $505K) — dispatch FIRST in the C-1/C-2/C-3 stack
2. **Conditional follow-up:** C-2 CANDIDATE-J Point-5 (drip-after-pause accrual) — gated on C-3 outcome; if Pattern E rounding-direction confirms attacker-extractable, the accrual surface is automatically Gate-2-worthy
3. **Lower priority:** C-1 CANDIDATE-J Point-4 (5-pause-flag halt-vs-direct) — strongest brain-compound value but lower Gate 2 EV (the architectural distinction is more useful for future Gate 1 calibration than for first-paste-ready submission)

**Beyond Compound III:** brain catalog Pick #3 (FRAX core refreshCollateralRatio + COLLATERAL_RATIO_PAUSER) is the next CANDIDATE-J Gate 1 candidate. Apply same Doctrine #27 tier classification first — FRAX audit count likely MEDIUM tier (5-10 audits, well below Aave V3/Spark MAXIMUM). FRAX Point-2 cooldown is the strongest test surface in the entire CANDIDATE-J target list per map line 112.

---

_Standing-Intake Gate 1 — Compound III (Comet) Immunefi | 2026-05-28 | autonomous Gate 1 dispatch | Doctrine #27 catalog 1697 MEDIUM-HIGH 0.40× confirmed | DC-7 EXCLUSION CANONICAL boundary documented | WATCHLIST-PARK + Gate-2-VIABLE on 3 candidates | 5 brain compound proposals filed | no clone (disk 85% / 5.4G) | Buzz Lane 1 Day 28_
